# 钉版前已知问题 triage（2026-08-06，18 跑冻结的前置）

状态：**盘点中**。钉 tag 前逐项处置（修复 / 声明为已知偏离 / 判不影响）。
对抗性代码审查（未知问题）进行中，发现追加至 §B。

## A. 已记录的（来源：审计/ISSUES/本会话）

| # | 问题 | 影响面 | 来源 |
|---|---|---|---|
| A1 | faithful 配方 `--solver o3d` 绕过论文 Eq.5 特征感知假设选择 | A 线方法身份（审计标 critical deviation） | REPRODUCTION §IV-A 审计。**已修（Vincent 拍板）**：faithful 换 `--solver gpu-feat`（`GPURansacSolver` fitness="feature" = 固定分母 Eq.5，iters 默认 10000，原生 k=10）；tuned 保留 o3d 作已声明调优项。runbook 同步删 `--corr-topk 10`（o3d 专属旋钮，非 o3d 会 ValueError；gpu 路径原生 k=10 覆盖论文值） |
| A2 | `M` 用数据集级最大多重度取底，**非 per-target N+1** | 多实例集（ITODD/IC-BIN/T-LESS）语义；阻塞 M=2N | 同上。**已修**：`floored_topk` 改 per-target `max(--topk, inst_count+1)`（Table V 的 M=N+1），`segment()` 增每次调用的 topk 覆盖；构造期不再抬地板。M=2N 从此只是系数变更 |
| A3 | §IV-A 审计基于 dirty `1d785b7`——执行路径四行（分割/稀疏采样/M/solver）须对钉版 commit 重核 | 审计结论有效性 | 同上 |
| A4 | `render_rerank.py:148` 按文件路径运行时加载 `scripts/sar_render_compare.py`，缺文件即崩 | 18 跑全部 | 盘点 agent。**已加闸**：四段 runbook 的存在检查扩为 `check_rerank_symmetry.py` + `sar_render_compare.py` 两文件（崩在跑前而非 20h 中途）；崩溃本身是 loud 的，不改加载方式 |
| A5 | `load_gedi` 语义反转：外部调用者用 `POPOE_GEOM_BACKBONE` 时静默得 GeDi | 外部脚本（仓内已迁完） | aa6fb2e |
| A6 | 官方 441 SAM6D 七份 + NIDS/MUSE/CNOS 新五集**从未过管线**（仅 schema 校验） | 四路 16 跑 | 本会话。**LM-O 已过**：faithful-4way obj1 smoke（gpu 主机 @ ec19a64）用 441+MUSE 官方文件端到端跑通，AR(2/3) 0.7486、S1 PASS、y_sign 健康锁存。**已全过（26b69e0 preflight5）**：五集各 3 targets 端到端 RC=0，四源候选数逐集非零（tless 3-4/itodd 10-12/hb 4-5/icbin 36-39/tudl 5-6）。A6 关闭 |
| A7 | `resolve_resume` 按行数续跑：旧 CSV 不挪走则瞬间"成功"留坏数据 | 重跑操作 | void 批教训 |
| A8 | 五个新集的 layout/models 假设未 preflight（T-LESS `models_cad`、ITODD/HB GT 形态等） | 广度 10 跑 | EXPERIMENTS 旧记。**已过（preflight5）**：tless models_cad 的 models_info 解析、itodd gray+float32 深度换算、hb test_primesense、icbin 多实例（3 targets=37 行，M=N+1+NMS 实战首过）、tudl——全部端到端 RC=0 |
| A9 | 本地测试环境缺 torch，torch 路径测试从未在本地跑过 | 回归盲区 | 本会话。**盲区已用双 smoke 部分覆盖**（gpu 主机 @ ec19a64）：回归锚 tuned-4way obj1 = 0.8166 vs 参照 0.8160（+0.0006，候选数 6880 逐位同，修复零扰动）；新路径 faithful-4way obj1 = 0.7486 首参考值（gpu-feat/官方四路/diameter+MIN_VIEWS=18 冷 cache/NMS 0.05/y_sign 门全部首真跑）。torch 单元测试仍只能在 GPU 主机跑 |
| A10 | scoring.py 注释方向已修（4f93a5f），Ch5 措辞待同步 | 文档 | C2 战役 |

## B. 对抗性审查发现（B1 = 正式跑代码通路，详表 `TRIAGE_20260806_pathB1.md`）

| # | 级别 | 问题 |
|---|---|---|
| B1-F1 | **blocks-pin** | **query cache 命中路径以错误基重建规范系**：`bop_eval.py:874` → `CanonFrame.from_points`（`freeze/adapters.py:129`）按缓存点的 `1/extent` 重建，而 faithful 钉（`POPOE_CANON_BASIS=diameter`+`MIN_VIEWS=18`）下缓存特征建于 `1/hull-diameter`——LM-O 上两基差 2–35%。暖 query/冷 target（崩溃续跑、smoke 后 full 共享 cache——REPRODUCTION 自己就这么规定）即得错尺度 GeDi 特征，且**以自称 diameter 的键写回缓存 = 持久污染**。tuned 臂只是巧合逃过（默认基下 from_points 恰好精确）。守卫无法区分两种基——与 tau 同型。**已修 `c156e90`**：写入侧把 `canon_scale`（float64）记进 query npz，命中侧 `restore_canon_frame()` 按记录恢复；无记录的旧条目仅在「extent 基 + 无可见性门」下按历史惯例回退，其余一律硬失败（旧缓存整体弃用）。回归测试 `tests/test_canon_frame_cache.py` 5 例。**review 波补修**：同源 bug 在 `scripts/flip_rescore_ab.py` 原样存活（正对 faithful 缓存踩），已按同规则修；残余风险（旧 checkout 对新 cache 写毒 target 条目，key 无从分辨）属 stale-restore 操作纪律，靠 `popoe.__file__` 正面确认防 |
| B1-F2 | must-disclose | `--use-s-coarse` × `--render-rerank` 组合：`s_coarse` 在 pre-ICP、pre-flip 的 R 上求值（`scoring.py:131-143`），重排翻正后的候选仍乘着被翻掉朝向的余弦——纠正过的候选反被自洽的未纠正竞争者压。两效应从未同测；S1 只审 `s_icp`，smoke 看不见。**已修（Vincent 拍板修而非披露）**：rerank 选中翻转变体时把同一模型系 delta（`D = Rᵀ·Rv`，re-ICP 前捕获）同步施加到 breakdown 的 `R_coarse`，S_coarse 从此测被打分的朝向族；冠军胜出时逐位不变。连带修 minor「re-ICP 异常保留陈旧 s_icp」：失败即回退冠军位姿（翻转 R 配 pre-flip s_icp 不可打分）。**review 波修正**：回退时 score/`sar_ti` 一并改回冠军自己的外观分（原实现留着被丢弃翻转的高分，scorer-less Pipeline 下位姿与得分错配）。回归测试 3 例 |
| B1-F3 | must-disclose | y_sign 标定从**首个冠军位姿一次性锁存**（`render_rerank.py:236-239` + `sar_render_compare.py:247-265`），首候选退化则整个 20h 进程全部镜像渲染打分，无日志无地板；smoke 是独立进程独立锁存，证明不了 full。**已加固**：标定 IoU < 0.3 不锁存（本次渲染用默认 +1，下个候选重试），锁存与拒锁均打日志（`[rerank] y_sign …`）；健康首候选行为逐位不变。阈值 0.3 pinned-by-us（正确符号在正常位姿上远高于此，错误符号 ~0）。门逻辑在 `_sar_ti` 内需 torch，本地测试环境无法执行——pod smoke 时看日志行验证 |
| B1-m | minor | rerank re-ICP 异常时翻转保留冠军旧 `s_icp`（标记不落 CSV）；`_tau_icp` 潜在 extent 基回退；`cache.py` 确定性 tmp 路径并行可撞（读侧响亮）；`ChampionScorer` 缺 `s_icp` 默认 0.0 |

B1 已核干净的：tau 修复（含 `--tau-diameter` 路径）、corr-topk 方向、seed 管道、
enc_cfg 键完备性 vs 全部 faithful env 钉、bbox 转换、PCA 基纪律、mm/m 全部转换、
resume 语义。**结论：F1 修复前不得钉版**（会污染缓存）；F2/F3 位姿/打分侧，可后置加固但须披露。

## B2 = 检测摄取与七集假设（详表 `TRIAGE_20260806_pathB2.md`；已对 20 份新文件真实载荷解码验证）

| # | 级别 | 问题 |
|---|---|---|
| B2-F1 | **blocks-pin** | **union 跨源重复吃掉 inst_count 名额**：`segmentor_detections.py:396-411` 设计上保留跨源重复，`adapters.py:134-146` 选实例无空间互斥——多实例集（icbin inst 至 19/tless/itodd）上同一物理实例被 2-4 源看到 = 占 2-4 个行槽，结构性封顶 recall。钉版前须定：inst_count>1 时选择端 IoU 抑制，或把该封顶预注册为 union 属性。**已修（按 C1 的论文忠实修法）**：`select_top_instances` 增 translation NMS（`nms_dist`，贪心 best-first、top-k 截断前、不回填），`bop_eval --trans-nms` 参数化（论文 §III-F 未给半径，pinned-by-us，Vincent 拍板参数化）。**review 波修正**：默认 0.1→**0.05**×直径（薄件/嵌套件——icbin 杯——真实实例中心距可小于 0.1d，同实例重复经 ICP 收敛在 ~1–2%d，0.05 仍有 2.5–5 倍余量）；加非有限平移预滤（一行 NaN 会把整靶后续冠军全压掉）；测试 +6 |
| B2-F2 | **blocks-pin** | **冻结表检测路径在本机之外全悬空**：28 份输入全被 gitignore，19 份是指向 `outputs/.../official_submissions/` 的绝对路径符号链接——pod 上 segmentor 初始化即 FileNotFoundError。冻结须含 `cp -L`/B2 物化 + 对 PROVENANCE 表核 SHA256。**已修**：`scripts/freeze_detections.py`（默认物化 + 校验；`--check` 为 pod 侧 preflight）。19 个 symlink 已物化为真文件，31 个 json 全对上登记（补建 cnos/PROVENANCE.md，登记 ISM 两份与 union 参考件哈希）。**review 波加固**：审查实测原版 `--check` 对空树/整目录丢失/目录级 symlink 全部放行，且集合成员校验过不了「两个已登记文件互换」与「546 归档件顶替 441 路径」——已加 `MANIFEST.sha256` 路径↔哈希绑定（tracked）、目录 symlink 拒收、零 json 硬失败、`--need` 按跑完整性门、tmp 残留检查、tmp 名带 pid；测试 11 例 |
| B2-F3 | must-disclose | M 底数用数据集级 max inst_count（A2 已知，现已量化）：E-4way 在 icbin 保 ~1.8 万掩码、tless 上界 ~11.1 万——运行时被底数支配；M≠N+1 阻塞 M=2N 声明。**随 A2 修复解除**：per-target N+1 后掩码预算随各 target 的 N 走，数据集级上界失效 |
| B2-F4 | must-disclose | itodd/hb test GT 官方扣留 → 该 4 跑无本地分数闸门；`--probe-corr` 在其上会崩 |
| B2-F5 | must-disclose | tless 无色 `models_cad` 渲染 + itodd 灰度照片使 DINO 半区离分布；**16-bit tif 会触发 `bop_eval.py:353-358` uint8 硬失败**——每集先过 1 图 preflight。**实测解除**：itodd gray 是 uint8 tif（16-bit 担忧不成立），深度是 float32 tif、换算路径 preflight 过；离分布（灰度照片/无色渲染）作为披露项保留 |
| B2-F6 | must-disclose | 五个新集数据树不在本机——layout 表对照过 bop_toolkit 但从未落盘验证。**已解除**：gpu 主机五套齐备且逐集验收（场景数/targets/models_info/图像格式），preflight5 落盘跑通 |

B2 核干净的（重要排除面）：20 份新文件全为规整未压缩 RLE、类别即 obj_id、
尺寸匹配各集分辨率、四源逐集图像覆盖一致、mask 解码对 bbox 交叉无转置、
`min_pixels=100` 每文件至多丢 1 条、per-image 恒定 time 使四 JSON 归一化精确、
MSPD 640/W 逐场景正确。**A6 的摄取半边就此解除**；A6 余下的是端到端跑通。

## C. 论文对照重写发现（gedi PIPELINE.md 重写，Vincent 2026-08-06）

| # | 级别 | 问题 |
|---|---|---|
| C1 | **重定 B2-F1 修法** | 论文候选协议本来就「不跨来源合并、不过滤重叠 mask」——去重发生在**末端 translation NMS**（§III-F），而 popoe 没有实现 translation NMS。故 B2-F1 的论文忠实修法 = 实现平移 NMS，而非选择端 IoU 抑制（后者是偏离）。**已按此实现**（见 B2-F1 行） |
| C2 | must-disclose | query 多视角聚合：论文只写 `weighted average` 未给权重公式；popoe 的可见性二值等权（`sum/visible_count`）须标 **pinned-by-us**，不得声称论文确认无法向夹角等权重 |
| C3 | **已核，干净** | Eq.6 `S_ICP` 分母 = `\|P_Q\|`（全部 query 点）。核验：`registration.py:146-166` `src_pcd` = query 云（`ICPRefiner.refine` 传 `query.pts`）、`tgt_pcd` = 稠密 target；o3d `result.fitness` 分母 = source 点数 = `\|P_Q\|`，同一 `tau_icp` 作对应距离——与论文一致，无需改动 |
| C4 | must-disclose | target 稀疏采样：论文 = 最小正方形 bbox + 16×16 patch 中心 + 中心落 mask 内 + **不插值**；popoe = 矩形 mask grid + 双线性取特征（审计已有行，语义现已钉准） |
| C5 | must-disclose | query 5k 预算：论文未明确是可见性过滤前还是过滤后（§IV-A 排 `P_Q^sparse` vs §III-D 只定义 `P_Q^raw→P_Q`）——本地选择必须显式声明 |
| C6 | must-disclose | 对应方向：论文 target→query top-k，o3d mutual_filter 连**方向**都不同（A1 的细化）。**faithful 路径随 A1 修复解决**：gpu 路径的匹配即「top-k query NNs per target point」（Eq.3 方向），mutual filter 默认关；o3d 的方向差异从此只属于 tuned 臂（已声明调优项） |
| C7 | 事实钉准 | Eq.7 不含 mask confidence——低置信 mask 可凭位姿质量胜出；detection 协议 M=100/τ_mask=0.4 未实现（A2 关联） |

## §D · 二轮审计（Vincent，@ 04d5121）——faithful 仍非论文等价，问题从 RANSAC 转移到打分/掩码/target 提取

| # | 级别 | 内容 | 处置 |
|---|---|---|---|
| D1 | **P1 最高** | **Eq.7 消费的不是 Eq.5 分数**：gpu_ransac 内部用固定分母 Eq.5 排序（`gpu_score`），但交给 ChampionScorer 的 `s_coarse` 被重算成几何最近邻 mean-cosine（`feature_aware_score`），`s_fine`（s_feat_1）同——当前 unit-exponent "Eq.7" 实为 mean_cos×mean_cos×S_ICP。论文原文：「S_feat^fine … using the same formulation provided in Eq.(5)」 | **已修 `707abd3`**：`registration.eq5_score`（镜像 gpu_ransac 构造）+ `ChampionScorer(eq5_terms)`，`--eq5-terms` 入 faithful 配方；默认关 = tuned 逐位不变。测试 6 例 |
| D2 | P1 | **掩码被额外过滤**：min_pixels=100 + 同源 IoU>0.9 去重 + 被滤不补足 M——非论文的 unfiltered union | **已修 `707abd3`**：`--min-mask-pixels` / `--mask-iou-dedupe` 开 CLI，faithful 配方传 0 / 1.1 关闭；默认不变 |
| D3 | P1 | **target 特征提取不等价**：矩形 bbox + linspace 端点网格 + DINO 图双线性插值 + 样本不足回退随机像素；论文=最小正方 bbox + 16×16 patch centers 直赋、不插值 | **已修 `1cca358`**：`POPOE_TARGET_PAPER_GRID=1`——方形 bbox + 16×16 patch 中心（floor 语义；round 的银行家舍入会重复像素并越界，测试钉死）+ 逐 patch 直赋不插值 + 不回退随机像素；几何在 `adapters.paper_grid_centers`（无 torch 可测）。入 enc_cfg 条件键与 faithful 配方 |
| D4 | P1 | **3k 稠密云被拆成两朵**：GeDi 邻域用 mask 全部有效深度，ICP 另建 ≤3k；论文 P_T^dense=3k 同时服务两者 | **已修 `1cca358`**：`POPOE_TARGET_DENSE=3000`——GeDi 邻域与 ICP 云同经 `adapters.fixed_seed_subsample`（同索引空间同种子），与 `--icp-dense-max 3000` 配对即逐位同一朵云。入 enc_cfg 条件键与 faithful 配方 |
| D5 | P1 | faithful 配置混入 `--render-rerank`（基础 Row 18 无 SAR）；且 faithful_eval.sh 与 faithful_run.sh 两个 wrapper 配置不一致（rerank/ico162） | wrapper 已统一（faithful_run.sh 补 ico162/N_VIEWS 钉、换 gpu-feat、同旗标组）。**已拍板（Vincent）：faithful 撤下 rerank**——基础 Row 18 无 SAR，rerank 是近似件只留 tuned 线（决策 9 修订）；四段 runbook + wrapper 已删旗标，Cautions 改写。⚠️ smoke 值 0.7486 是**带 rerank** 跑的旧配方，仅管线活性证据。**新身份首参考值 = 0.7231**（AR(2/3)，obj1 LM-O @ 26b69e0：无 rerank + eq5-terms + unfiltered union + paper grid + 单一 3k 云；MSSD 0.6851/MSPD 0.7611，176 行 RC=0） |
| D6 | P1 | 6D detection 协议（M=100/τ_mask=0.4/未知实例数/AP 流程）未实现 | 记录为范围外（18 跑均为 localization），不修 |
| D7 | P2 | 近似/未披露项：sample_surface_even≠严格 Poisson、5k 先采后滤、476≠480、DINOv2 层/PCA/二值可见性等权、α=β=γ=1、NMS 0.05、M=2N 无动态配置、rerank≠官方 SAR | 全部按 pinned-by-us 逐项登记于 REPRODUCTION（多数已有行，补齐缺的） |
| D8 | — | §IV-A 审计表基于旧 commit，未含 D1/D2 两发现 | 并入 A3：终版 commit 上全表重核 |

### §D review 波（对抗性 agent @ 3a301dd 前）

- eq5_score 与 gpu_ransac torch 构造做了真 A/B 数值对拍（venv 有 CPU torch）：300 试验最大分差 4.5e-8、inlier 零错配；D4 双云在合成数据上逐字节相等。核心逻辑站住。
- 实锤已修（`3a301dd`）：**F1** crop round() 与连续 tiling 错位半像素、特征指派最坏整格全错——方框角点单点 floor 定整，crop 精确复现；**F2** 1×1 掩码 256 重复中心过 len 门——改 ≥4 唯一像素；**F3** `POPOE_TARGET_DENSE`≠`--icp-dense-max` 静默裂回两朵云——启动拒跑；**F4**（流程）`test_eq5_terms.py` 此前从未提交，commit 测试数不可复现——已入库；**F6** faithful_run.sh 的 SEED 1234 漂移/缺 unset 卫生块/缺 cand-csv——对齐；**F7b** paper-grid 开关与 enc_cfg 键判据统一为 `!= "0"`。
- 记录为已知（不改）：**F7a** 两个 target 旋钮进 query key（多作废 query 缓存，无正确性影响，faithful 全新 cache 无感）；**F8** eq5 模式下 s_feat_w 的对照池随空间变化（诊断列不进分）；**F9** eq5 分数可 >1、cand.csv 无模式标记（靠 FRESH --out 纪律，与 --tau-diameter 同先例）。

## §E · 三轮审计（Vincent，@ ~04d5121 后）——运行流程与渲染几何

| # | 级别 | 内容 | 处置 |
|---|---|---|---|
| E-P1 | P1 流程 | faithful 两 wrapper 固定输出名却不查已存在，叠加 resume 只按行数分类（A7）——沿用旧 OUT 会静默保留修复前位姿、看起来像新代码跑完 | **已修 `f551866`**：两 wrapper 输出已存在即拒启（exit 3）；runbook 四段本就有同款拒启环 |
| E-P2 | P1 | `POPOE_QUERY_FILL` 是**无效旋钮**：mesh 预缩放 ∝ fill 与相机距离 ∝ 缩放后 extent 相消，60° FOV 下物体恒占画面 ~0.577，0.45/0.5 都改不了 | **已修 `f551866`**：`query_camera_radius` 增 `effective` 模式（r = extent/(fill·2·tan(fov/2))，fill 成为真实占比设定），`POPOE_QUERY_FILL_MODE` 门控 + enc_cfg 条件键，默认 legacy 逐位不变（保护全部历史缓存键）；faithful 钉 effective。⚠️ faithful 身份再变：smoke 参考 0.7231 是 legacy fill 跑的，**终版身份定格后须重取 smoke 参考** |
| E-P3 | 文档 | REPRODUCTION 四处与代码脱节（审计头仍称 1d785b7、target 行仍称矩形+双线性、rerank 仍称全配方启用、3k 共享云待办未勾） | **已修 `f551866`**：全部同步（target 行 Match-on-faithful、rerank 限定 tuned、3k 勾销、fill 行补相消事实） |
