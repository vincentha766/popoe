# 钉版前已知问题 triage（2026-08-06，18 跑冻结的前置）

状态：**盘点中**。钉 tag 前逐项处置（修复 / 声明为已知偏离 / 判不影响）。
对抗性代码审查（未知问题）进行中，发现追加至 §B。

## A. 已记录的（来源：审计/ISSUES/本会话）

| # | 问题 | 影响面 | 来源 |
|---|---|---|---|
| A1 | faithful 配方 `--solver o3d` 绕过论文 Eq.5 特征感知假设选择 | A 线方法身份（审计标 critical deviation） | REPRODUCTION §IV-A 审计 |
| A2 | `M` 用数据集级最大多重度取底，**非 per-target N+1** | 多实例集（ITODD/IC-BIN/T-LESS）语义；阻塞 M=2N | 同上 |
| A3 | §IV-A 审计基于 dirty `1d785b7`——执行路径四行（分割/稀疏采样/M/solver）须对钉版 commit 重核 | 审计结论有效性 | 同上 |
| A4 | `render_rerank.py:148` 按文件路径运行时加载 `scripts/sar_render_compare.py`，缺文件即崩 | 18 跑全部 | 盘点 agent |
| A5 | `load_gedi` 语义反转：外部调用者用 `POPOE_GEOM_BACKBONE` 时静默得 GeDi | 外部脚本（仓内已迁完） | aa6fb2e |
| A6 | 官方 441 SAM6D 七份 + NIDS/MUSE/CNOS 新五集**从未过管线**（仅 schema 校验） | 三路/四路 16 跑 | 本会话 |
| A7 | `resolve_resume` 按行数续跑：旧 CSV 不挪走则瞬间"成功"留坏数据 | 重跑操作 | void 批教训 |
| A8 | 五个新集的 layout/models 假设未 preflight（T-LESS `models_cad`、ITODD/HB GT 形态等） | 广度 10 跑 | EXPERIMENTS 旧记 |
| A9 | 本地测试环境缺 torch，torch 路径测试从未在本地跑过 | 回归盲区 | 本会话 |
| A10 | scoring.py 注释方向已修（4f93a5f），Ch5 措辞待同步 | 文档 | C2 战役 |

## B. 对抗性审查发现（B1 = 正式跑代码通路，详表 `TRIAGE_20260806_pathB1.md`）

| # | 级别 | 问题 |
|---|---|---|
| B1-F1 | **blocks-pin** | **query cache 命中路径以错误基重建规范系**：`bop_eval.py:874` → `CanonFrame.from_points`（`freeze/adapters.py:129`）按缓存点的 `1/extent` 重建，而 faithful 钉（`POPOE_CANON_BASIS=diameter`+`MIN_VIEWS=18`）下缓存特征建于 `1/hull-diameter`——LM-O 上两基差 2–35%。暖 query/冷 target（崩溃续跑、smoke 后 full 共享 cache——REPRODUCTION 自己就这么规定）即得错尺度 GeDi 特征，且**以自称 diameter 的键写回缓存 = 持久污染**。tuned 臂只是巧合逃过（默认基下 from_points 恰好精确）。守卫无法区分两种基——与 tau 同型 |
| B1-F2 | must-disclose | `--use-s-coarse` × `--render-rerank` 组合：`s_coarse` 在 pre-ICP、pre-flip 的 R 上求值（`scoring.py:131-143`），重排翻正后的候选仍乘着被翻掉朝向的余弦——纠正过的候选反被自洽的未纠正竞争者压。两效应从未同测；S1 只审 `s_icp`，smoke 看不见 |
| B1-F3 | must-disclose | y_sign 标定从**首个冠军位姿一次性锁存**（`render_rerank.py:236-239` + `sar_render_compare.py:247-265`），首候选退化则整个 20h 进程全部镜像渲染打分，无日志无地板；smoke 是独立进程独立锁存，证明不了 full |
| B1-m | minor | rerank re-ICP 异常时翻转保留冠军旧 `s_icp`（标记不落 CSV）；`_tau_icp` 潜在 extent 基回退；`cache.py` 确定性 tmp 路径并行可撞（读侧响亮）；`ChampionScorer` 缺 `s_icp` 默认 0.0 |

B1 已核干净的：tau 修复（含 `--tau-diameter` 路径）、corr-topk 方向、seed 管道、
enc_cfg 键完备性 vs 全部 faithful env 钉、bbox 转换、PCA 基纪律、mm/m 全部转换、
resume 语义。**结论：F1 修复前不得钉版**（会污染缓存）；F2/F3 位姿/打分侧，可后置加固但须披露。

（B2 = 检测摄取与新集假设，回报待追加）
